import styled from 'styled-components';

const TableHeadStyles = styled.div`
  .order-card-header {
    align-items: center;
    background-color: #eaf1ef;
    display: flex;
    font-weight: 600;
    justify-content: space-between;
    margin: 0 1rem;
    padding: 1rem 2rem;
    text-transform: uppercase;
`;

export default TableHeadStyles;
